DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=adminpassword

SECRET_KEY="django-insecure-lp6h18zq4@z30symy*oz)+hp^uoti48r_ix^qc-m@&yfxd7&hn"
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500
CORS_ALLOWED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500

# Leave empty locally -> SQLite fallback kicks in automatically.
# Only set on Render (Neon connection string):
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Leave empty locally -> local in-memory cache kicks in automatically, RQ
# jobs are skipped (no local Redis needed for testing).
# Only set on Render (Upstash connection string):
# REDIS_URL=rediss://default:password@host:port

EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_google_app_password
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=your_email@gmail.com

FRONTEND_URL=http://127.0.0.1:5500
